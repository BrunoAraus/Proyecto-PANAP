import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CambiarInformacionNegPage } from './cambiar-informacion-neg.page';

describe('CambiarInformacionNegPage', () => {
  let component: CambiarInformacionNegPage;
  let fixture: ComponentFixture<CambiarInformacionNegPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CambiarInformacionNegPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
